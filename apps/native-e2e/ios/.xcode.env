# Versioned environment used by React Native's Xcode bundle phase.
# Put machine-specific overrides in the ignored `.xcode.env.local` file.
export NODE_BINARY=$(command -v node)
export ENTRY_FILE=${ENTRY_FILE:-index.tsx}
