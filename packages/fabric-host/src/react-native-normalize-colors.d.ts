/** Minimal ABI used by Fabric Host's transport-safe property encoder. */
declare module "@react-native/normalize-colors" {
  export default function normalizeColor(color: string | number): number | null;
}
